<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoffeeJournal">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CoffeeJournal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RecordActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeJournal"/>
        <activity
            android:name=".activities.FavouritesActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeJournal"/>
        <activity
            android:name=".activities.CoffeeInfoActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeJournal"/>
        <activity
            android:name=".activities.RecipesActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeJournal"/>
        <activity
            android:name=".activities.FlavourWheelActivity"
            android:exported="true"
            android:theme="@style/Theme.CoffeeJournal"/>
    </application>

</manifest>